// Arrow function statement body
const sum = (a, b) => {
    const result = a + b;

    //тука може да има if(), for()... и т.н.

    return result;
} 

// Arrow function expression body
const sum2 = (a, b) => a + b;

// Expressioon body arrow function with single parameter
const double = (a) => a * 2; //когато е само един параметър може и без скоби


